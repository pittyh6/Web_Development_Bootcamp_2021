<%- include('partials/header') -%>

    <h1>Home</h1>
    <p><%= startingContent %></p>

    <!-- New Post -->
   <% posts.forEach(function(post){%>
    <% console.log(post.titleTxt) %>
   <% }) %>

<%- include("partials/footer")-%>